document.getElementById("loginForm")?.addEventListener("submit", function (e) {
  e.preventDefault();
  alert("✅ লগইন সফল (ডেমো)");
});

document.getElementById("signupForm")?.addEventListener("submit", function (e) {
  e.preventDefault();
  alert("✅ সাইন আপ সফল (ডেমো)");
});
